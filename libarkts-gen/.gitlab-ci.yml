idlize sanity:
  stage: test
  tags:
    - idlize-ci
    - koala-ci
  before_script:
    - !reference [.setup, script]
  script:
    - npm run subset --prefix libarkts
    - npm run full -- prefix libarkts

